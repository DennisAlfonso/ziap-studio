<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="ZiapStudio.Views.FusionWorldDocumentView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:world="using:ZiapStudio.Core.Fusion.World">

    <Grid Padding="32,26">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid ColumnSpacing="18">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Spacing="4">
                <StackPanel Orientation="Horizontal" Spacing="10">
                    <TextBlock FontSize="30" FontWeight="SemiBold" Text="World &amp; Navigation" />
                    <Border
                        Padding="8,3"
                        VerticalAlignment="Center"
                        Background="{ThemeResource AccentFillColorSecondaryBrush}"
                        CornerRadius="5">
                        <TextBlock FontSize="10" FontWeight="SemiBold" Text="READ ONLY" />
                    </Border>
                </StackPanel>
                <TextBlock
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Text="Dipendenze di tileset, mappe e asset RPG Maker; diagnostica senza modificare il progetto." />
            </StackPanel>
            <StackPanel Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center" Spacing="3">
                <TextBlock HorizontalAlignment="Right" FontWeight="SemiBold" Text="{Binding WorkspaceStatusText}" />
                <TextBlock
                    HorizontalAlignment="Right"
                    FontSize="11"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Text="{Binding InventoryText}" />
            </StackPanel>
        </Grid>

        <TabView
            Grid.Row="1"
            Margin="0,18,0,0"
            CanReorderTabs="False"
            HorizontalContentAlignment="Stretch"
            IsAddTabButtonVisible="False"
            TabWidthMode="SizeToContent"
            VerticalContentAlignment="Stretch">
            <TabViewItem Header="Tilesets" IsClosable="False">
                <Grid Margin="0,12,0,0" ColumnSpacing="18">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="300" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Border
                        Padding="14"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="10">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <StackPanel Spacing="3">
                                <TextBlock FontSize="18" FontWeight="SemiBold" Text="Tileset Manager" />
                                <TextBlock FontSize="11" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="Uso effettivo dalle MapXXX.json." />
                            </StackPanel>
                            <ListView
                                Grid.Row="1"
                                Margin="0,12,0,0"
                                HorizontalContentAlignment="Stretch"
                                ItemsSource="{Binding Tilesets}"
                                SelectedItem="{Binding SelectedTileset, Mode=TwoWay}"
                                SelectionMode="Single">
                                <ListView.ItemTemplate>
                                    <DataTemplate x:DataType="world:FusionWorldTileset">
                                        <Grid Padding="4,6" ColumnSpacing="9">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="30" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock FontFamily="Consolas" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" Text="{x:Bind IdText}" />
                                            <StackPanel Grid.Column="1" Spacing="1">
                                                <TextBlock FontWeight="SemiBold" Text="{x:Bind Name}" TextTrimming="CharacterEllipsis" />
                                                <TextBlock FontSize="10" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="{x:Bind MapCountText}" />
                                            </StackPanel>
                                            <TextBlock Grid.Column="2" VerticalAlignment="Center" FontSize="10" Text="{x:Bind StatusText}" />
                                        </Grid>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </Grid>
                    </Border>
                    <Grid Grid.Column="1" RowSpacing="14">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <Grid ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Spacing="2">
                                <TextBlock FontSize="21" FontWeight="SemiBold" Text="{Binding SelectedTileset.Name}" />
                                <TextBlock FontSize="11" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="{Binding SelectedTilesetStatusText}" />
                            </StackPanel>
                            <TextBlock Grid.Column="1" VerticalAlignment="Center" FontFamily="Consolas" Text="{Binding SelectedTileset.IdText}" />
                        </Grid>
                        <Grid Grid.Row="1" ColumnSpacing="14">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Border
                                Padding="14"
                                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                BorderThickness="1"
                                CornerRadius="10">
                                <Grid>
                                    <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
                                    <TextBlock FontSize="17" FontWeight="SemiBold" Text="Slot sheet" />
                                    <ListView Grid.Row="1" Margin="0,10,0,0" ItemsSource="{Binding SelectedTilesetSlots}" SelectionMode="None">
                                        <ListView.ItemTemplate>
                                            <DataTemplate x:DataType="world:FusionWorldTilesetSlot">
                                                <Grid Padding="3,6" ColumnSpacing="10">
                                                    <Grid.ColumnDefinitions><ColumnDefinition Width="34" /><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                                                    <TextBlock FontFamily="Consolas" FontWeight="SemiBold" Text="{x:Bind Slot}" />
                                                    <TextBlock Grid.Column="1" FontFamily="Consolas" FontSize="11" Text="{x:Bind DisplayPath}" TextTrimming="CharacterEllipsis" />
                                                    <TextBlock Grid.Column="2" FontSize="10" Text="{x:Bind StatusText}" />
                                                </Grid>
                                            </DataTemplate>
                                        </ListView.ItemTemplate>
                                    </ListView>
                                </Grid>
                            </Border>
                            <Border
                                Grid.Column="1"
                                Padding="14"
                                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                BorderThickness="1"
                                CornerRadius="10">
                                <Grid>
                                    <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
                                    <TextBlock FontSize="17" FontWeight="SemiBold" Text="Usato da" />
                                    <ListView Grid.Row="1" Margin="0,10,0,0" ItemsSource="{Binding SelectedTilesetMaps}" SelectionMode="None">
                                        <ListView.ItemTemplate>
                                            <DataTemplate x:DataType="world:FusionWorldMapUsage">
                                                <StackPanel Padding="3,5" Spacing="1">
                                                    <TextBlock FontWeight="SemiBold" Text="{x:Bind DisplayText}" TextTrimming="CharacterEllipsis" />
                                                    <TextBlock FontSize="10" Foreground="{ThemeResource TextFillColorTertiaryBrush}" Text="{x:Bind SourcePath}" TextTrimming="CharacterEllipsis" />
                                                </StackPanel>
                                            </DataTemplate>
                                        </ListView.ItemTemplate>
                                    </ListView>
                                </Grid>
                            </Border>
                        </Grid>
                    </Grid>
                </Grid>
            </TabViewItem>

            <TabViewItem Header="Assets" IsClosable="False">
                <Grid Margin="0,12,0,0" ColumnSpacing="18">
                    <Grid.ColumnDefinitions><ColumnDefinition Width="360" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                    <Border Padding="14" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="10">
                        <Grid>
                            <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
                            <StackPanel Spacing="3">
                                <TextBlock FontSize="18" FontWeight="SemiBold" Text="Asset Usage" />
                                <TextBlock FontSize="11" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="{Binding AssetAuditText}" />
                            </StackPanel>
                            <ListView Grid.Row="1" Margin="0,12,0,0" HorizontalContentAlignment="Stretch" ItemsSource="{Binding Assets}" SelectedItem="{Binding SelectedAsset, Mode=TwoWay}" SelectionMode="Single">
                                <ListView.ItemTemplate>
                                    <DataTemplate x:DataType="world:FusionWorldAsset">
                                        <StackPanel Padding="4,6" Spacing="2">
                                            <TextBlock FontFamily="Consolas" FontSize="11" FontWeight="SemiBold" Text="{x:Bind AssetPath}" TextTrimming="CharacterEllipsis" />
                                            <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><TextBlock FontSize="10" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="{x:Bind UsageText}" /><TextBlock Grid.Column="1" FontSize="10" Text="{x:Bind StatusText}" /></Grid>
                                        </StackPanel>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </Grid>
                    </Border>
                    <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto">
                        <StackPanel Spacing="14">
                            <Grid ColumnSpacing="14"><Grid.ColumnDefinitions><ColumnDefinition Width="260" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                                <Border Width="260" Height="210" Padding="8" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="10"><Image Source="{Binding SelectedAssetPreview}" Stretch="Uniform" /></Border>
                                <StackPanel Grid.Column="1" Spacing="5"><TextBlock FontSize="21" FontWeight="SemiBold" Text="{Binding SelectedAsset.AssetPath}" TextWrapping="Wrap" /><TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="{Binding SelectedAssetStatusText}" /><TextBlock FontSize="11" Text="{Binding SelectedAsset.SizeText}" /><TextBlock FontFamily="Consolas" FontSize="10" Foreground="{ThemeResource TextFillColorTertiaryBrush}" Text="{Binding SelectedAsset.HashText}" TextTrimming="CharacterEllipsis" /><TextBlock FontSize="10" Foreground="{ThemeResource TextFillColorTertiaryBrush}" Text="{Binding SelectedAsset.PhysicalPath}" TextWrapping="Wrap" /></StackPanel>
                            </Grid>
                            <Grid ColumnSpacing="14"><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                                <Border Padding="14" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="10"><Grid><Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions><TextBlock FontSize="17" FontWeight="SemiBold" Text="Tileset che lo referenziano" /><ListView Grid.Row="1" Margin="0,10,0,0" ItemsSource="{Binding SelectedAssetTilesets}" SelectionMode="None"><ListView.ItemTemplate><DataTemplate x:DataType="world:FusionWorldTileset"><TextBlock Padding="3,5" Text="{x:Bind Name}" /></DataTemplate></ListView.ItemTemplate></ListView></Grid></Border>
                                <Border Grid.Column="1" Padding="14" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="10"><Grid><Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions><TextBlock FontSize="17" FontWeight="SemiBold" Text="Mappe interessate" /><ListView Grid.Row="1" Margin="0,10,0,0" ItemsSource="{Binding SelectedAssetMaps}" SelectionMode="None"><ListView.ItemTemplate><DataTemplate x:DataType="world:FusionWorldMapUsage"><TextBlock Padding="3,5" Text="{x:Bind DisplayText}" /></DataTemplate></ListView.ItemTemplate></ListView></Grid></Border>
                            </Grid>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </TabViewItem>

            <TabViewItem Header="Maps" IsClosable="False">
                <Border Margin="0,12,0,0" Padding="14" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="10">
                    <Grid><Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
                        <StackPanel Spacing="3"><TextBlock FontSize="18" FontWeight="SemiBold" Text="Mappe e tileset assegnati" /><TextBlock FontSize="11" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="Ogni MapXXX.json è collegata al suo tileset effettivo." /></StackPanel>
                        <ListView Grid.Row="1" Margin="0,12,0,0" ItemsSource="{Binding Maps}" SelectionMode="None"><ListView.ItemTemplate><DataTemplate x:DataType="world:FusionWorldMap"><Grid Padding="4,7" ColumnSpacing="14"><Grid.ColumnDefinitions><ColumnDefinition Width="78" /><ColumnDefinition Width="*" /><ColumnDefinition Width="260" /></Grid.ColumnDefinitions><TextBlock FontFamily="Consolas" Text="{x:Bind IdText}" /><TextBlock Grid.Column="1" FontWeight="SemiBold" Text="{x:Bind DisplayName}" /><TextBlock Grid.Column="2" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="{x:Bind TilesetText}" /></Grid></DataTemplate></ListView.ItemTemplate></ListView>
                    </Grid>
                </Border>
            </TabViewItem>

            <TabViewItem Header="Diagnostics" IsClosable="False">
                <Border Margin="0,12,0,0" Padding="14" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="10">
                    <Grid><Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
                        <StackPanel Spacing="3"><TextBlock FontSize="18" FontWeight="SemiBold" Text="Asset Auditor" /><TextBlock FontSize="11" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="Mancanti, orfani, duplicati, mappe e definizioni non valide. Le stesse regole alimentano Pre-Flight." /></StackPanel>
                        <ListView Grid.Row="1" Margin="0,12,0,0" ItemsSource="{Binding Diagnostics}" SelectionMode="None"><ListView.ItemTemplate><DataTemplate x:DataType="world:FusionWorldDiagnostic"><StackPanel Padding="4,7" Spacing="3"><StackPanel Orientation="Horizontal" Spacing="9"><TextBlock FontWeight="SemiBold" Text="{x:Bind SeverityText}" /><TextBlock FontFamily="Consolas" FontSize="10" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="{x:Bind Code}" /></StackPanel><TextBlock Text="{x:Bind Message}" TextWrapping="Wrap" /><TextBlock FontSize="10" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="{x:Bind Details}" TextWrapping="Wrap" /></StackPanel></DataTemplate></ListView.ItemTemplate></ListView>
                    </Grid>
                </Border>
            </TabViewItem>
        </TabView>
    </Grid>
</UserControl>
